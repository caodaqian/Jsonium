**状态栏图标（仅 emoji 替换）执行计划**

背景与目标
- 你要求最小改动：仍使用 emoji，但需要在窄窗口下更容易区分各功能，尤其“转义 / 反转义”。
- 目标：通过最小的结构与样式改动，让不同功能的 emoji 在任意宽度下容易辨识；保持现有事件与逻辑不变，零外部依赖。

约束
- 只修改 `src/components/StatusBar.vue`（template + style）；非必须不改其他文件。
- 不改变事件处理或现有行为（例如 `escapeJson`, `unescapeJson` 等）。
- 兼容主题变量（使用现有 CSS 变量或安全默认值）。

实现（最小可行改动）
1) 模板改动（StatusBar.vue）
   - 把直接放 emoji 的节点替换为带类名的包裹元素：
     - 在按钮内部，把 `<span class="action-btn__icon">🔒</span>` 改为
       `<span class="action-btn__icon emoji-badge emoji-badge--escape" aria-hidden="true">🔒</span>`，并为按钮添加 `title` 和 `aria-label`。
   - 为 escape/unescape 按钮分别添加类：`action-btn--escape` / `action-btn--unescape`。
   - 把按钮文本改为短文本（在模板中直接写“转”/“反”），以便在窄屏时仍然保留辨识信息。

   示意替换（原 -> 新，最小量）：
   - 原（示意）:
     ```vue
     <button @click="escapeJson" class="action-btn action-btn--icon" title="转义">
       <span class="action-btn__icon">🔒</span><span class="action-btn__text">转义</span>
     </button>
     ```

   - 新（示意）:
     ```vue
     <button @click="escapeJson"
             class="action-btn action-btn--icon action-btn--escape"
             title="转义"
             aria-label="转义 (escape)">
       <span class="action-btn__icon emoji-badge emoji-badge--escape" aria-hidden="true">🔒</span>
       <span class="action-btn__text">转</span>
     </button>

     <button @click="unescapeJson"
             class="action-btn action-btn--icon action-btn--unescape"
             title="反转义"
             aria-label="反转义 (unescape)">
       <span class="action-btn__icon emoji-badge emoji-badge--unescape" aria-hidden="true">🗝️</span>
       <span class="action-btn__text">反</span>
     </button>
     ```

   - 其它按钮（格式化、复制、对比、AI、表格、设置）也建议统一使用 `emoji-badge` 类并带 `aria-label`。示例映射（建议，但可调整）：
     - 格式化：📐 -> `emoji-badge--format`
     - 复制：📋 -> `emoji-badge--copy`
     - 对比：⚖️ -> `emoji-badge--compare`
     - AI：🤖 -> `emoji-badge--ai`
     - 表格：📊 -> `emoji-badge--table`
     - 设置：⚙️ -> `emoji-badge--settings`

2) 样式改动（最小 CSS）
   - 在 `StatusBar.vue` 的 `<style scoped>` 下添加：
     ```css
     /* 小方块背景让 emoji 更容易识别 */
     .emoji-badge {
       display: inline-grid;
       place-items: center;
       width: 28px;
       height: 28px;
       border-radius: 6px;
       background: var(--color-badge-bg, rgba(0,0,0,0.04));
       color: inherit; /* 保持 emoji 本色或可覆盖 */
       font-size: 14px;
     }

     /* 关键操作颜色（示例） */
     .emoji-badge--escape { background: var(--color-escape, #f59e0b); color: #fff; }
     .emoji-badge--unescape { background: var(--color-unescape, #10b981); color: #fff; }

     /* 窄屏时隐藏大多数按钮文本，但保留关键按钮短文本 */
     @media (max-width: 900px) {
       .action-btn__text { display: none; }
       .action-btn.action-btn--escape .action-btn__text,
       .action-btn.action-btn--unescape .action-btn__text {
         display: inline-block;
         font-size: 11px;
         margin-left: 6px;
       }
     }
     ```

3) 无需改动逻辑
   - 不改动 `escapeJson` / `unescapeJson` 的实现；仅改变 template 与样式。

4) 可选测试更新
   - 在 `src/__tests__/statusBar.spec.js` 中增加简单断言：关键按钮存在 `title` 与 `aria-label`，并带上 `action-btn--escape` / `action-btn--unescape` 类。

5) 验证步骤
   - 本地运行 dev server：`npm run dev`。
   - 在三种宽度下（1200px / 900px / 480px）手工检查：
     - 转/反按钮通过色块与不同 emoji 可被迅速识别；
     - 其它按钮外观统一；
     - 无功能回归（快捷键与事件仍正常）。

改动清单（最终）
- 必要：`src/components/StatusBar.vue`（template + style）
- 可选：`src/__tests__/statusBar.spec.js`（断言增强）

估时
- 编辑模板与样式并完成本地验证：30–60 分钟。
- 含测试与额外微调：+15–30 分钟。

执行指令（下一步）
- 我已把计划更新为“仅 emoji 替换”并列出精确修改步骤。
- 若你确认，我将按本计划实现改动（在你允许我离开 Plan Mode 后进行实际代码修改）。

用户反馈（界面现状）
- 你反馈当前实现带来了横向滚动条，视觉重量偏大，且希望按钮默认无持续背景，主要通过图标与悬浮提示来区分。

迭代目标（本次变更）
- 取消状态栏的水平滚动：让状态栏内容自适应窗口宽度并在必要时换行（不出现水平滚动条）。
- 移除按钮的持续背景（保持透明），用图标为主；鼠标悬浮或键盘聚焦时显示可见提示（tooltip 或微交互高亮）。
- 保持无功能改动：只改 template 与样式，不改事件或逻辑。

最小可行实现（实现细节）
1) 布局：避免横向滚动
   - 修改 `src/components/StatusBar.vue` 的样式：
     - 把 `.status-bar-row` 的 `flex-wrap: nowrap; overflow-x: auto;` 替换为 `flex-wrap: wrap; overflow-x: hidden;`，使右侧按钮在空间不足时自动换到下一行而不是出现滚动条。
     - 把 `.actions-inline` 设置为 `flex-wrap: wrap; gap: 6px; flex: 0 1 auto; margin-left: auto;`，保持它在可用空间内换行并且仍靠右对齐。
   - 确保查询输入 `.query-input` 可以缩小（设置 `min-width: 140px`）以避免强制溢出。

2) 外观：移除持续背景，保留 hover/focus 反馈
   - 把 `.action-btn` 默认样式改为透明：`background: transparent; border: none;`，去掉圆角背景。
   - 把原先的 `.emoji-badge` 背景清除：`.emoji-badge { background: transparent; width: auto; height: auto; padding: 0 4px; }`，让 emoji 本身呈现为图标。
   - 在 `:hover` / `:focus-visible` 时显示一种轻量反馈（可选：ring 或轻微背景），示例：
     `.action-btn:hover, .action-btn:focus-visible { background: var(--color-action-hover, rgba(0,0,0,0.04)); border-radius:6px; }`
   - 关键操作（转 / 反）仍保留 `aria-label` 与 `title` 以便无障碍与原生 tooltip 提示。

3) Tooltip：鼠标悬浮显示文字提示（视觉优先）
   - 复用现有 `.action-btn__text` 作为 tooltip 内容（默认隐藏），在 hover / focus 时显示为浮层：
     ```css
     .action-btn { position: relative; }
     .action-btn__text { display: none; position: absolute; bottom: calc(100% + 8px); left: 50%; transform: translateX(-50%); background: var(--color-bg-primary); border: 1px solid var(--color-border); padding: 6px 8px; border-radius: 6px; white-space: nowrap; box-shadow: var(--shadow-lg); font-size: var(--font-size-xs); z-index: 200000; }
     .action-btn:hover .action-btn__text, .action-btn:focus-visible .action-btn__text { display: block; }
     @media (min-width: 1100px) { .action-btn__text { display: inline-block; position: static; padding: 0; border: none; box-shadow: none; } }
     ```
   - 说明：`title` / `aria-label` 仍留作回退及屏幕阅读器支持；CSS tooltip 提供一致的视觉样式。

4) 不改动行为
   - 不更改 `escapeJson` / `unescapeJson` / `formatJson` 等函数，只改 template + style。

实施步骤（我将做的事）
1. 更新 `src/components/StatusBar.vue`：
   - Template：保持 `emoji-badge` 标签，但移除默认彩色背景（只留类），并确保每个按钮包含 `title` 与 `aria-label`。
   - Style（scoped）：
     - 修改 `.status-bar-row`、`.actions-inline` 的换行与溢出规则；
     - 覆盖 `.action-btn` 背景为透明并添加 hover/focus 高亮；
     - 修改 `.emoji-badge` 为无背景与紧凑 padding；
     - 添加 CSS tooltip（基于 `.action-btn__text`）。
2. 本地验证：运行 `npm run dev`，在 1200 / 900 / 480px 下核验：
   - 无横向滚动条；按钮自动换行或压缩；
   - 按钮默认无背景；hover/focus 显示 tooltip 或浅色高亮；
   - 功能与快捷键不受影响。

测试与回滚
- 推荐手动验证为主（视觉改动）；可选在 `src/__tests__/statusBar.spec.js` 中断言 `aria-label` 不变。
- 若视觉不满意，可回滚样式变更（简单撤销 commit）。

估时
- 实现并本地验证：20–40 分钟。

下一步（需要你的确认）
- 请确认：我可以按此迭代（自动换行 + 移除背景 + hover tooltip）开始实现改动并在本地验证。确认后我将离开 Plan Mode 并修改 `src/components/StatusBar.vue`。
